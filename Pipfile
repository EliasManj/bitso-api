[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
grequests = "*"
requests = "*"

[scripts]
install_packages = "sh -c 'cd engines && pip install -e .'"

[dev-packages]

[requires]
python_version = "3.11"
python_full_version = "3.11.7"
